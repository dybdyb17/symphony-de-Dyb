{% extends 'base.html.twig' %}

{% block title %}Modifier user{% endblock %}

{% block body %}
    <h1>Modifier l'utilisateur {{ user.username }}</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}

{% endblock %}
